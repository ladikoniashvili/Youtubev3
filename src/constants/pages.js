const HOME_PAGE = "/";
const SEARCH_RESULT = "/searchResult/:searchQuery";
const VIDEO_ID = "/video/:id";
const REGISTER = "/signup";
const SIGN_IN = "/signin";
const ERROR = "/error";

const my_pages = {
  HOME_PAGE,
  SEARCH_RESULT,
  VIDEO_ID,
  REGISTER,
  SIGN_IN,
  ERROR,
};

export default my_pages;
